# 只保留 Compose 基础变量，不要把数据库连接和鉴权地址写在这里
COMPOSE_PROJECT_NAME=kite
KITE_PORT=3000
KITE_IMAGE=kite:local

# 更新脚本默认会 git pull --ff-only；临时跳过可设置 true
KITE_SKIP_GIT_PULL=false

# 更新脚本默认会执行 prisma migrate deploy；临时跳过可设置 true
KITE_SKIP_MIGRATE=false
